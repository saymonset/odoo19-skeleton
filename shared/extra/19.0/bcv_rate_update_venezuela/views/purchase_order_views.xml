<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Vista tree base (lista simple) con Total USD -->
    <record id="purchase_order_tree_view_usd" model="ir.ui.view">
        <field name="name">purchase.order.tree.usd.total</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_tree"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_total']" position="after">
                <field name="cop_show_fields" column_invisible="True"/>
                <field name="amount_total_usd" string="Total USD" sum="Total USD" widget="monetary" options="{'currency_field': 'currency_aux'}"/>
                <field name="amount_total_cop" string="Total COP" optional="hide" widget="monetary" options="{'currency_field': 'currency_aux_cop'}" invisible="not cop_show_fields"/>
            </xpath>
        </field>
    </record>

    <!-- Vista tree KPIs (dashboard de solicitudes) con Total COP -->
    <record id="purchase_order_kpis_tree_view_usd" model="ir.ui.view">
        <field name="name">purchase.order.kpis.tree.usd.total</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_kpis_tree"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_total']" position="after">
                <field name="cop_show_fields" column_invisible="True"/>
                <field name="amount_total_usd" string="Total USD" sum="Total USD" widget="monetary" options="{'currency_field': 'currency_aux'}"/>
                <field name="amount_total_cop" string="Total COP" optional="hide" widget="monetary" options="{'currency_field': 'currency_aux_cop'}" invisible="not cop_show_fields"/>
            </xpath>
        </field>
    </record>

    <!-- Vista form: columnas USD en líneas + Total USD -->
    <record id="purchase_order_form_view_usd" model="ir.ui.view">
        <field name="name">purchase.order.form.usd.total</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="price_tier_type"/>
            </xpath>
            <!-- Columnas en las líneas -->
            <xpath expr="//field[@name='order_line']/list/field[@name='price_subtotal']" position="before">
                <field name="cop_show_fields" column_invisible="True"/>
                <field name="rate_value" string="Tasa BCV (USD/VES)" readonly="1" optional="show"/>
                <field name="rate_value_cop" string="Tasa COP" readonly="1" optional="hide" invisible="not cop_show_fields"/>
                <field name="price_subtotal_usd_bcv" string="Subtotal USD" readonly="1"/>
                <field name="price_subtotal_cop" string="Subtotal COP" readonly="1" optional="hide" invisible="not cop_show_fields"/>
            </xpath>
            <!-- Total USD en el footer -->
            <xpath expr="//group[hasclass('oe_subtotal_footer')]" position="inside">
                <field name="cop_show_fields" invisible="1"/>
                <div style="display:flex; align-items:center; gap:5px;" class="mt-2">
                    <label for="amount_total_usd" string="Total USD (BCV)" style="font-weight:bold; color:green;"/>
                    <field name="amount_total_usd" readonly="1" style="font-weight:bold; color:green;" widget="monetary" options="{'currency_field': 'currency_aux'}"/>
                </div>
                <div style="display:flex; align-items:center; gap:5px;">
                    <label for="bcv_rate_value" string="Tasa BCV (USD/VES)" style="font-weight:bold;"/>
                    <field name="bcv_rate_value" readonly="1" style="font-weight:bold;"/>
                </div>
                <div style="display:flex; align-items:center; gap:5px;" class="mt-1" invisible="not cop_show_fields">
                    <label for="amount_total_cop" string="Total COP" style="font-weight:bold; color:blue;"/>
                    <field name="amount_total_cop" readonly="1" style="font-weight:bold; color:blue;" widget="monetary" options="{'currency_field': 'currency_aux_cop'}"/>
                </div>
            </xpath>
        </field>
    </record>

</odoo>
