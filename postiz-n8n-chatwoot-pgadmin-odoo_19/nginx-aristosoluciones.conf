# ============================================================
# ARISTO SOLUCIONES - integraia.lat (169.58.138.60)
# Reverse proxy nginx para el stack docker (odoo, odoo-leads,
# chatwoot, n8n, postiz, pgadmin, temporal-ui)
# ============================================================

# ---------- Upstreams ----------
upstream odoo {
    server 127.0.0.1:18069;
}

upstream odoo_longpolling {
    server 127.0.0.1:18072;
}

upstream odoo_leads {
    server 127.0.0.1:28069;
}

upstream odoo_leads_longpolling {
    server 127.0.0.1:28072;
}

upstream chatwoot {
    server 127.0.0.1:3000;
}

upstream n8n {
    server 127.0.0.1:5678;
    keepalive 32;
}

upstream postiz {
    server 127.0.0.1:4007;
}

upstream pgadmin {
    server 127.0.0.1:8080;
}

upstream temporal_ui {
    server 127.0.0.1:8180;
}

# ============================================================
# DOMINIO PRINCIPAL aristosoluciones.integraia.lat -> Odoo
# ============================================================
server {
    listen 80;
    server_name aristosoluciones.integraia.lat;

    include snippets/letsencrypt.conf;

    location / {
        return 301 https://aristosoluciones.integraia.lat$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name aristosoluciones.integraia.lat;

    ssl_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aristosoluciones.integraia.lat/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/chain.pem;
    include snippets/ssl.conf;
    include snippets/letsencrypt.conf;

    access_log /var/log/nginx/aristosoluciones-odoo.access.log;
    error_log /var/log/nginx/aristosoluciones-odoo.error.log;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;

    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;

    location ~ \.css$ {
        proxy_pass http://odoo;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        add_header Content-Type "text/css" always;
    }

    location ~* /web/static/ {
        proxy_pass http://odoo;
        proxy_cache_valid 200 90m;
        proxy_buffering on;
        expires 864000;
        add_header Cache-Control "public, immutable";
    }

    location /websocket {
        proxy_pass http://odoo_longpolling;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_connect_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    location /longpolling/ {
        proxy_pass http://odoo_longpolling;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 900s;
        proxy_connect_timeout 900s;
        proxy_send_timeout 900s;
    }

    # Bloqueo API RPC deprecated (vector del ataque 2026-08-17, IP 208.84.103.154)
    location ^~ /xmlrpc  { return 403; }
    location ^~ /jsonrpc { return 403; }
    location ^~ /web/database { return 403; }

    location / {
        proxy_pass http://odoo;
        proxy_redirect off;
    }
}

# ============================================================
# SUBDOMINIO LEADS lead.aristosoluciones.integraia.lat -> Odoo Leads
# ============================================================
server {
    listen 80;
    server_name lead.aristosoluciones.integraia.lat;

    include snippets/letsencrypt.conf;

    location / {
        return 301 https://lead.aristosoluciones.integraia.lat$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name lead.aristosoluciones.integraia.lat;

    ssl_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aristosoluciones.integraia.lat/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/chain.pem;
    include snippets/ssl.conf;
    include snippets/letsencrypt.conf;

    underscores_in_headers on;
    client_max_body_size 50M;

    access_log /var/log/nginx/aristosoluciones-odoo-leads.access.log;
    error_log /var/log/nginx/aristosoluciones-odoo-leads.error.log;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;

    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;

    location ~ \.css$ {
        proxy_pass http://odoo_leads;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        add_header Content-Type "text/css" always;
    }

    location ~* /web/static/ {
        proxy_pass http://odoo_leads;
        proxy_cache_valid 200 90m;
        proxy_buffering on;
        expires 864000;
        add_header Cache-Control "public, immutable";

        location ~ \.css$ {
            add_header Content-Type "text/css" always;
        }
    }

    location /websocket {
        proxy_pass http://odoo_leads_longpolling;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_connect_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    location /longpolling/ {
        proxy_pass http://odoo_leads_longpolling;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 900s;
        proxy_connect_timeout 900s;
        proxy_send_timeout 900s;
    }

    location ^~ /xmlrpc  { return 403; }
    location ^~ /jsonrpc { return 403; }
    location ^~ /web/database { return 403; }

    location / {
        proxy_pass http://odoo_leads;
        proxy_redirect off;
    }
}

# ============================================================
# chatwoot.aristosoluciones.integraia.lat
# ============================================================
server {
    listen 80;
    server_name chatwoot.aristosoluciones.integraia.lat;

    include snippets/letsencrypt.conf;

    location / {
        return 301 https://chatwoot.aristosoluciones.integraia.lat$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name chatwoot.aristosoluciones.integraia.lat;

    ssl_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aristosoluciones.integraia.lat/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/chain.pem;
    include snippets/ssl.conf;
    include snippets/letsencrypt.conf;

    underscores_in_headers on;
    client_max_body_size 50M;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;

    location /rails/active_storage/ {
        proxy_pass http://chatwoot;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Ssl on;
        expires 1y;
        add_header Cache-Control "public, immutable";
        proxy_buffering off;
        proxy_request_buffering off;
    }

    location /cable {
        proxy_pass http://chatwoot;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_read_timeout 86400s;
    }

    location / {
        proxy_pass http://chatwoot;
        proxy_redirect off;
    }
}

# ============================================================
# n8n.aristosoluciones.integraia.lat
# ============================================================
server {
    listen 80;
    server_name n8n.aristosoluciones.integraia.lat;

    include snippets/letsencrypt.conf;

    location / {
        return 301 https://n8n.aristosoluciones.integraia.lat$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name n8n.aristosoluciones.integraia.lat;

    ssl_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aristosoluciones.integraia.lat/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/chain.pem;
    include snippets/ssl.conf;
    include snippets/letsencrypt.conf;

    underscores_in_headers on;
    client_max_body_size 50M;

    access_log /var/log/nginx/n8n.access.log;
    error_log /var/log/nginx/n8n.error.log;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;

    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;

    location / {
        proxy_pass http://n8n;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_redirect off;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        proxy_pass http://n8n;
        proxy_cache_valid 200 90m;
        proxy_buffering on;
        expires 10d;
        add_header Cache-Control "public";
    }

    gzip on;
    gzip_types text/css text/less text/plain text/xml application/xml application/json application/javascript;
    gzip_vary on;
    gzip_min_length 1024;
}

# ============================================================
# postiz.aristosoluciones.integraia.lat
# ============================================================
server {
    listen 80;
    server_name postiz.aristosoluciones.integraia.lat;

    include snippets/letsencrypt.conf;

    location / {
        return 301 https://postiz.aristosoluciones.integraia.lat$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name postiz.aristosoluciones.integraia.lat;

    ssl_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aristosoluciones.integraia.lat/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/chain.pem;
    include snippets/ssl.conf;
    include snippets/letsencrypt.conf;

    underscores_in_headers on;
    client_max_body_size 50M;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;

    location /rails/active_storage/ {
        proxy_pass http://postiz;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Ssl on;
        expires 1y;
        add_header Cache-Control "public, immutable";
        proxy_buffering off;
        proxy_request_buffering off;
    }

    location /cable {
        proxy_pass http://postiz;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_read_timeout 86400s;
    }

    location ~* \.(jpg|jpeg|png|gif|ico|webp)$ {
        proxy_pass http://postiz;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        add_header Content-Security-Policy "default-src 'none'; img-src 'self' https: data:; media-src 'self' https:; style-src 'none'; script-src 'none'; frame-ancestors 'none'; sandbox";
        expires 1y;
        add_header Cache-Control "public, immutable";
        proxy_buffering off;
    }

    location / {
        proxy_pass http://postiz;
        proxy_redirect off;
    }
}

# ============================================================
# pgadmin.aristosoluciones.integraia.lat
# ============================================================
server {
    listen 80;
    server_name pgadmin.aristosoluciones.integraia.lat;

    include snippets/letsencrypt.conf;

    location / {
        return 301 https://pgadmin.aristosoluciones.integraia.lat$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name pgadmin.aristosoluciones.integraia.lat;

    ssl_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aristosoluciones.integraia.lat/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/chain.pem;
    include snippets/ssl.conf;
    include snippets/letsencrypt.conf;

    underscores_in_headers on;
    client_max_body_size 50M;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;

    location / {
        proxy_pass http://pgadmin;
        proxy_redirect off;
    }
}

# ============================================================
# temporal.aristosoluciones.integraia.lat -> temporal-ui
# ============================================================
server {
    listen 80;
    server_name temporal.aristosoluciones.integraia.lat;

    include snippets/letsencrypt.conf;

    location / {
        return 301 https://temporal.aristosoluciones.integraia.lat$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name temporal.aristosoluciones.integraia.lat;

    ssl_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aristosoluciones.integraia.lat/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/aristosoluciones.integraia.lat/chain.pem;
    include snippets/ssl.conf;
    include snippets/letsencrypt.conf;

    underscores_in_headers on;
    client_max_body_size 50M;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;

    location / {
        proxy_pass http://temporal_ui;
        proxy_http_version 1.1;
        proxy_redirect off;
    }
}
